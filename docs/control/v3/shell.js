const NAV=[['overview','index.html'],['modules','modules.html'],['configs','configs.html'],['archives','archives.html'],['telemetry','telemetry.html'],['actions','actions.html']];function nav(){const n=document.getElementById('nav');if(!n)return;const p=document.body.dataset.page;NAV.forEach(([id,href])=>{const a=document.createElement('a');a.href=href;a.textContent=id;a.className=id===p?'active':'';n.appendChild(a);});}async function load(path){const r=await fetch(path);if(!r.ok)throw new Error('load failed '+path);return r.json();}function card(label,value,extra){const d=document.createElement('div');d.className='item';d.innerHTML='<div class="label">'+label+'</div><div class="value">'+value+'</div>'+(extra?'<div class="muted">'+extra+'</div>':'');return d;}function log(id,lines){const node=document.getElementById(id);if(!node)return;const t=new Date().toLocaleTimeString();node.textContent=lines.map(x=>'['+t+'] '+x).join('\n')+'\n\n'+node.textContent;}window.KM={load,card,log};document.addEventListener('DOMContentLoaded',nav);
